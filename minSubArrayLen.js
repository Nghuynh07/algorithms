//write a function accepts an array of positive integers, and a positive value
//function should return the minimal length of a contiguous subarray of which the sum
//is greater than or equal to the integer passed to the function.if there isn't one return zero

function minSubArrayLen(array, n) {}

console.log(minSubArrayLen([2, 3, 1, 2, 4, 3], 7));
